# Project memory

Project notes: decisions and context that are not derivable from the code.
One fact per file, with frontmatter (`name`, `description`, `metadata.type`).
Project and reference memories live here so every machine sees them.

- [zen-space-router state](zen-space-router-state.md) — release state and what remains open
